name: 🧪 CI - Check
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - "*"
  workflow_call:
jobs:
  check:
    name: 🧪 Check
    uses: cyberskill-world/.github/.github/workflows/check.yml@main

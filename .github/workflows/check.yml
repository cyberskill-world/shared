name: 🧪 CI - Check
on:
  push:
    branches:
      - develop
  pull_request:
    branches:
      - "*"
jobs:
  check:
    name: 🧪 Check
    uses: cyberskill-world/.github/.github/workflows/check.yml@main

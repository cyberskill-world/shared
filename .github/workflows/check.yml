name: 🧪 CI - Check

on:
  pull_request:
    branches:
      - develop
      - staging
      - main
  push:
    branches:
      - develop
      - staging
      - main
  workflow_dispatch:

jobs:
  check:
    uses: cyberskill-world/.github/.github/workflows/check.yml@main

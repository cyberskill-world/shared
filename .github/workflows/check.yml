name: 🧪 CI - Check
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - "*"
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: cyberskill-world/.github/.github/workflows/check.yml@main
      - name: 📤 Upload build or dist folder as artifact
        uses: actions/upload-artifact@v4
        with:
          name: BUILD_ARTIFACT
          path: dist

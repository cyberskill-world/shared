name: 🚀 CI - Deploy Then
on:
  # workflow_run:
  #   workflows: ["🧪 CI - Check"]
  #   types:
  #     - completed
  workflow_dispatch:
jobs:
  deploy-then:
    # if: >
    #   github.event.workflow_run.conclusion == 'success' &&
    #   github.event.workflow_run.head_branch == 'main'
    runs-on: ubuntu-latest

    steps:
      - name: Trigger another repository's workflow
        uses: cyberskill-world/.github/actions/trigger-events@main
        with:
          REPO: "cyberskill-world/template-be-nodejs"
          EVENTS: "📈 CI - Update"
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
